# Astro & Obsidian 블로그 구축 및 운용 마스터 가이드

이 문서는 사용자가 `gksdydwn.github.io` 블로그를 직접 세팅하며 질문한 핵심 이론, Astro 프로젝트의 내부 구조, 실전 글쓰기 방법, 그리고 Obsidian 연동 과정까지 집대성한 통합 매뉴얼입니다.

---

## 목차
1. [Astro 블로그 디렉토리 및 파일 역할 정의](#1-astro-블로그-디렉토리-및-파일-역할-정의)
2. [Astro 실전 사용법 (포스팅 및 이미지 추가)](#2-astro-실전-사용법-포스팅-및-이미지-추가)
3. [Git & GitHub Actions 핵심 이론 및 흐름 요약](#3-git--github-actions-핵심-이론-및-흐름-요약)
4. [구축 중 발생한 에러 및 의문점 해결 (Q&A)](#4-구축-중-발생한-에러-및-의문점-해결-qa)
5. [Obsidian(옵시디언) 연동 및 활용 스텝-바이-스텝](#5-obsidian옵시디언-연동-및-활용-스텝-바이-스텝)

---

## 1. Astro 블로그 디렉토리 및 파일 역할 정의

Astro 프로젝트 폴더 안에는 여러 폴더와 파일들이 유기적으로 연결되어 있습니다. 각 구성 요소의 역할은 다음과 같습니다.

```text
git_blog/ (블로그 루트)
├── .github/workflows/
│   └── deploy.yml          # [자동 배포] 깃 push 시 깃허브 서버에서 돌릴 빌드/배포 스크립트
├── public/                 # [정적 자산] 파비콘, 폰트 파일 등 가공이 필요 없는 파일 보관함
│   └── favicon.svg         # 웹브라우저 탭에 표시될 사이트 아이콘
├── src/                    # [소스 코드] 실제 사이트가 제작되는 메인 소스 영역
│   ├── assets/             # [이미지/미디어] 포스트 본문에 활용할 이미지 압축/가공용 보관소
│   ├── components/         # [UI 구성요소] Header, Footer 등 화면 구석구석을 채울 조각 코드들
│   ├── content/            # [콘텐츠] 블로그에 발행되는 원본 글방
│   │   ├── config.ts       # 메타데이터 규격 검증 규칙 정의 (Zod 타입 체크 설정)
│   │   └── blog/           # 실제 포스팅용 마크다운(.md, .mdx) 파일들을 모아두는 곳
│   ├── layouts/            # [틀 디자인] 글 페이지의 전체 레이아웃 (BlogPost.astro 등)
│   ├── pages/              # [페이지 주소] 도메인 뒤에 붙는 URL 주소 경로를 형성하는 파일들
│   │   ├── index.astro     # 메인 페이지 (https://gksdydwn.github.io)
│   │   ├── about.astro     # 자기소개 페이지 (https://gksdydwn.github.io/about)
│   │   └── blog/           # 블로그 목록 및 글 상세 페이지들을 라우팅
│   └── styles/             # [스타일] 블로그 전체에 사용될 글로벌 CSS 디자인 설정 파일
├── package.json            # [부품 목록] 이 프로젝트를 구동하기 위해 컴퓨터가 내려받을 부품(라이브러리) 명세서
├── astro.config.mjs        # [아스트로 설정] 빌드 구성 요소를 정의하는 아스트로 엔진 설정 파일
└── tsconfig.json           # [타입스크립트 설정] 자바스크립트 코드 타입을 검사하는 규칙 정의서
```

---

## 2. Astro 실전 사용법 (포스팅 및 이미지 추가)

### ✍️ 포스팅 추가 및 템플릿 규격 (Frontmatter)
새로운 포스팅을 추가할 때는 반드시 `src/content/blog/` 폴더 아래에 `[파일명].md`로 파일을 생성하고, 파일 맨 위에 아래와 같은 규칙 정보(Frontmatter)를 적어주어야 배포 에러가 나지 않습니다.

```markdown
---
title: "포스트 제목을 적습니다"
description: "포스트에 대한 짧은 한 줄 설명을 적습니다."
pubDate: "2026-08-20"                           # 작성 연-월-일 (형식 엄수!)
heroImage: "../../assets/blog-placeholder-1.jpg" # 대표 이미지의 상대 경로
tags: ["Study", "Python"]                       # 카테고리/태그 목록
---

# 본문 제목
여기에 실제 마크다운 문법으로 글을 작성해 나가시면 됩니다.
```

### 🖼️ 본문에 이미지 추가 및 호출 경로
블로그 포스트 본문에 이미지를 삽입할 때는 두 가지 방식이 있습니다.

#### [권장] 1) `src/assets/` 폴더에 넣고 가공해 쓰기 (최적화)
* **이미지 저장 위치**: `src/assets/my-image.png`
* **마크다운 본문 내 작성 방식 (상대경로)**:
  `src/content/blog/` 폴더에 있는 마크다운 파일 기준으로 한 단계 위(`..`), 또 한 단계 위(`..`)에 있는 `assets` 폴더를 가리켜야 합니다.
  ```markdown
  ![이미지설명](../../assets/my-image.png)
  ```
  * *장점*: Astro가 빌드할 때 이미지 용량을 자동으로 압축 및 모바일 전용 크기로 가공하여 사이트 로딩 속도를 극대화합니다.

#### 2) `public/` 폴더에 넣고 정적 주소로 쓰기 (단순 참조)
* **이미지 저장 위치**: `public/images/my-image.png`
* **마크다운 본문 내 작성 방식 (루트경로)**:
  `public` 폴더는 빌드 시 사이트의 메인 최상위 루트로 1:1 복사되므로 `/`로 시작하는 절대 주소를 적으면 됩니다.
  ```markdown
  ![이미지설명](/images/my-image.png)
  ```
  * *장점*: 폴더 깊이 계산(상대경로)을 할 필요가 없어 사용이 편하지만, 이미지 용량이 압축되지 않아 속도 최적화가 누락됩니다.

---

## 3. Git & GitHub Actions 핵심 이론 및 흐름 요약

### 🚚 Git 저장 과정 비유 (택배 서비스 구조)
1. **`git add .`** (택배 상자에 물건 담기): 
   내가 로컬에서 수정한 내역들을 모아 임시 대기 구역(Staging Area)에 모아 넣는 과정입니다.
2. **`git commit -m "설명"`** (박스 밀봉 및 송장 부착): 
   상자를 닫고 송장을 부착하여 내 로컬 컴퓨터 데이터베이스(`.git` 폴더)에 찰칵 스냅샷으로 영구 기록합니다. (내 현관문 앞에 쌓아둠)
3. **`git push origin main`** (택배 기사님 발송 트럭 출발): 
   현관문 앞의 변경 사항 스냅샷들을 깃허브 웹 서버(Origin)로 실제로 압축 전송하여 동기화합니다.

### 🤖 GitHub Actions 자동 빌드 및 배포 흐름
1. **감지**: 깃허브가 push 신호를 받으면 저장소의 `.github/workflows/deploy.yml` 설정 지시를 읽습니다.
2. **부팅**: 깃허브 가상 서버가 부팅되고, 사전에 정의해 둔 최신 Node.js 22 LTS 환경을 세팅합니다.
3. **빌드**: 원본 마크다운 파일과 Astro 코드를 합쳐 고속 로딩이 가능한 완전한 정적 HTML 파일 묶음(`dist` 폴더)으로 굽습니다.
4. **배포**: 완성본을 웹 호스팅 서버 공간에 얹어 `https://gksdydwn.github.io`로 1분 안에 공개합니다.

---

## 4. 구축 중 발생한 에러 및 의문점 해결 (Q&A)

### Q. `Set-ExecutionPolicy` 보안 경고는 왜 났던 건가요?
* **A**: Windows PowerShell은 검증되지 않은 외부 스크립트 실행을 방지하기 위해 기본적으로 실행 권한을 엄격히 제한합니다. `RemoteSigned` 권한을 부여하여 로컬에서 직접 작성한 스크립트는 원활하게 돌아가도록 세팅한 것입니다.

### Q. `deploy.yml` 파일에 Node.js 버전을 꼭 고정해야 하나요?
* **A**: 버전 번호를 `node-version: "22"`와 같이 고정해 주어야 세월이 지나 Node.js 신버전이 나왔을 때 생길 수 있는 예기치 못한 호환성 에러로 빌드가 터지는 것을 예방합니다. 빌드 안정성의 핵심 표준입니다.

### Q. Astro가 "폴더가 비어 있지 않다"고 멈췄던 이유는 무엇인가요?
* **A**: 깃허브에서 클론을 받아온 `git_blog` 폴더 내부에는 이미 `.git` 설정 파일과 기본 README 등이 채워져 있었습니다. Astro 설치기가 기존 코드의 파괴를 우려하여 안전을 이유로 정지했던 것이며, 임시 폴더에 설치 후 내용물을 강제 이동하는 기법으로 우회 해결했습니다.

---

## 5. Obsidian(옵시디언) 연동 및 활용 스텝-바이-스텝

아직 한 번도 쓰지 않은 **Obsidian**을 블로그와 실전 연동하여 1초 만에 포스팅 글을 배포하는 방법입니다.

### ① Obsidian 설치 및 저장소 폴더 열기
1. [옵시디언 홈페이지(https://obsidian.md)](https://obsidian.md)에서 윈도우 설치 프로그램을 설치하고 실행합니다.
2. 옵시디언 첫 화면에서 **"Open folder as vault"** (폴더를 보관소로 열기) 단추를 클릭합니다.
3. 내 컴퓨터의 블로그 글방 주소인 **`c:\Users\User_\Desktop\클로드폴더\Code\git_blog`** 폴더를 선택하여 엽니다.

### ② 옵시디언 내에서 글 작성하기
1. 옵시디언 왼쪽 탐색기 창에서 `src/content/blog/` 폴더를 클릭하여 이동합니다.
2. 마우스 우클릭 -> **New note**를 생성합니다. (예: `python-study.md`)
3. 마크다운으로 깔끔하게 노트를 작성합니다.
   * *장점*: 옵시디언 환경에서는 본문에 이미지를 `Ctrl+V`로 갖다 붙이기만 하면 알아서 `src/assets` 등의 디렉토리로 이미지가 복사되어 저장되도록 설정할 수 있습니다.

### ③ Obsidian Git 플러그인을 활용한 1초 배포 자동화
옵시디언 내에서 글을 다 쓰고 나면 복잡한 터미널 명령어 없이 단축키나 클릭만으로 원클릭 push를 할 수 있게 설정합니다.

1. 옵시디언 왼쪽 아래 **설정(톱니바퀴 ⚙️)** -> **Community plugins** -> **Enable community plugins** 버튼 클릭.
2. **Browse** 버튼을 누르고 **`Obsidian Git`**을 검색하여 설치(Install)하고 활성화(Enable)합니다.
3. 플러그인 옵션(Options)으로 들어갑니다.
   * `Vault backup interval (minutes)`: 설정 시간(예: 30분)마다 백그라운드에서 수정한 메모들을 깃허브로 자동 백업합니다. (0으로 두면 자동 백업 비활성화)
   * `Commit message`: 자동 커밋이 전송될 때 쓸 기본 문구를 설정합니다. (기본값: `vault backup: {{date}}`)
4. **글 수동 발행 단축키 지정**:
   * 옵시디언 설정 -> **Hotkeys** 메뉴로 이동합니다.
   * `Obsidian Git: Commit and push all changes` 검색을 한 뒤, 내가 편한 단축키(예: `Ctrl + Shift + P`)를 설정해 둡니다.
5. 이제 글을 쓰고 저장한 후 설정해 둔 단축키를 누르면, 터미널을 열 필요 없이 **옵시디언이 알아서 백그라운드에서 push를 끝마치고, 1분 뒤 인터넷 블로그에 내 글이 자동으로 반영**됩니다!

---

## 6. 옵시디언 생산성 극대화 세팅 꿀팁 (Astro 맞춤형)

옵시디언을 사용하여 공부 내용을 "가장 쉽고 똑똑하게" 정리하기 위해 처음에 꼭 해두어야 할 핵심 세팅 2가지입니다.

### 🖼️ ① 이미지 붙여넣기(Ctrl+V) 자동 저장 위치 맞추기 (매우 중요!)
원래 옵시디언에 이미지를 복사-붙여넣기하면 메모 파일과 같은 폴더에 이미지 파일이 마구잡이로 생겨 폴더가 지저분해집니다. 이를 Astro의 이미지 보관소인 `src/assets/` 폴더로 바로 들어가도록 설정합니다.

1. 옵시디언 왼쪽 아래 **설정(톱니바퀴 ⚙️)**을 클릭합니다.
2. **Files and links** (파일 및 링크) 메뉴로 이동합니다.
3. **Default location for new attachments** (새 첨부파일의 기본 경로) 설정을 찾습니다.
4. 값을 **"In the folder specified below"** (아래에 지정된 폴더)로 변경합니다.
5. 바로 밑에 나타나는 **Attachment folder path** (첨부파일 폴더 경로) 칸에 **`src/assets`** 를 입력합니다.
6. **결과**: 이제 인터넷에서 유용한 그림을 캡처하고 옵시디언 노트 본문에서 `Ctrl+V`를 누르기만 하면, 이미지 파일이 자동으로 `src/assets/` 폴더에 깔끔하게 저장되며, 본문 마크다운에도 자동으로 이미지 링크가 이쁘게 생성됩니다!

### 📋 ② 블로그 글 상단 규격(YAML) 템플릿 등록하기
매번 글을 쓸 때마다 날짜와 형식(`title`, `description`, `heroImage` 등)을 직접 타이핑하는 것은 매우 귀찮은 일입니다. 단축키 하나로 이 규격을 본문에 쓱 불러오도록 세팅해 둡니다.

1. `git_blog` 루트 폴더 내에 임의로 **`_templates`** 라는 폴더를 하나 만듭니다. (옵시디언 탐색기에서 만드셔도 됩니다.)
2. 그 폴더 안에 **`blog-post-template.md`** 라는 새 노트를 만들고 아래 양식을 그대로 적어 둡니다.
   ```markdown
   ---
   title: "{{title}}"
   description: "이 포스트에 대한 설명"
   pubDate: "{{date}}"
   heroImage: "../../assets/blog-placeholder-about.jpg"
   tags: ["Study"]
   ---
   ```
3. 옵시디언 **설정 ⚙️** -> **Core plugins** (기본 플러그인) 메뉴로 갑니다.
4. **Templates** (템플릿) 플러그인이 켜져(활성화) 있는지 확인하고, 그 옆의 톱니바퀴 설정을 클릭합니다.
5. **Template folder path** 칸에 방금 만든 **`_templates`** 폴더를 입력합니다.
6. **Date format** (날짜 형식) 칸에 **`YYYY-MM-DD`** 를 입력합니다. (Astro 표준 날짜 포맷)
7. **사용 방법**: 
   * 앞으로 새 노트를 만들고 빈 화면에서 **`Alt + T`** (또는 단축키 등록)를 누르면, 오늘 날짜(`pubDate`)와 파일명이 제목(`title`)에 자동으로 주입된 완성형 블로그 포맷이 1초 만에 불러와집니다!

