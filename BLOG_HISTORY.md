# 블로그 구축 이력 & 인계 리포트

> 다음 세션이나 다른 AI 어시스턴트가 이 블로그의 현재 상태와 설계 의도를 **빠르게 파악**하기 위한 문서입니다.
> 루트의 마크다운 파일은 Astro 빌드에 아무 영향을 주지 않으므로 그대로 저장소에 두어도 됩니다.
>
> 최종 갱신: 2026-08-22

---

## 1. 기본 정보

| | |
| --- | --- |
| 프레임워크 | **Astro 7.x** (`astro@^7.2.3`) |
| 로컬 경로 | `c:\Users\User_\Desktop\클로드폴더\Code\git_blog` |
| 운영 도메인 | https://gksdydwn.github.io |
| 저장소 | https://github.com/gksdydwn/gksdydwn.github.io |
| 배포 | `main`에 push → GitHub Actions 자동 빌드·배포 |
| 글쓰기 도구 | Obsidian (이 폴더를 vault로 사용) |

실행 명령과 글쓰기 규칙은 [BLOG_GUIDE.md](./BLOG_GUIDE.md)에 정리돼 있습니다.

---

## 2. 콘텐츠 구조 — 3층 분류

**이 블로그에서 가장 중요한 설계 결정입니다.**

```
category      Study / Project / Interest 중 하나   (필수, 글당 1개)
subcategory   자유 문자열                          (선택)
tags          자유 문자열 배열                      (검색용, 여러 개)
```

### 왜 나눴나

처음에는 전부 `tags` 한 칸에 넣었습니다. 그런데 태그 목록을 보니 성격이 다른 두 가지가 섞여 있었습니다.

- `Study`, `Project` — "이 글이 **어떤 종류의** 글인가" → 서랍 (글당 하나)
- `Astro`, `Obsidian` — "이 글이 **무엇에 관한** 글인가" → 꼬리표 (여러 개)

둘을 한 칸에 두니 좌측 트리에서 **같은 글이 여러 서랍에 동시에 나타났습니다.** 글이 1편인데 태그가 6개라 폴더 6개에 전부 같은 글이 들어가 있었습니다.

### 왜 `z.enum`인가

`category`를 `z.enum(['Study','Project','Interest'])`으로 고정했습니다.

- 오타(`Projct`)를 **배포 전에 빌드가 파일 이름과 함께 잡아줍니다**
- 글당 정확히 1개라 트리 중복이 구조적으로 불가능합니다
- 안 적으면 빌드가 멈춰서 미분류 글이 안 생깁니다

소분류는 반대로 자유 문자열입니다. `Python`, `CS`처럼 **계속 늘어나는 성격**이라 목록을 고정하면 글 쓸 때마다 설정 파일을 열어야 하기 때문입니다.

분류를 늘리려면 `src/content.config.ts`의 배열에 단어를 추가하면 됩니다. 기존 글은 건드릴 필요가 없습니다.

### 대분류가 메인 위젯과 연결됨

`Study` → 최근 학습 노트 / `Project` → 소프로젝트 진행 현황(`completionRate` 게이지) / `Interest` → 관심사

---

## 3. 화면 구조

### 3단 위키 레이아웃

```
[ 좌 200px ]  [ 중앙 1fr (본문 820px) ]  [ 우 180px ]
  분류 트리         글 / 대시보드            목차·정보
```

- 컨테이너 `max-width: 1320px`, `gap: 1.2rem`, 양옆에 얇은 세로 구분선
- 배경은 슬레이트 그레이(`#f1f5f9`), 본문은 순백 카드(`.content-card`)로 띄워 입체감
- 반응형: `1024px` 이하 우측 숨김 → `768px` 이하 좌측도 숨김
- **주의**: 이 그리드 CSS가 `index.astro`, `blog/index.astro`, `BlogPost.astro` **3곳에 중복**돼 있습니다. 공용 레이아웃으로 뽑을 여지가 있지만, 화면이 바뀔 위험이 있어 미뤄두었습니다

### 좌측 — `CategoryTree.astro`

`category` → `subcategory` → 글 순서로 **2단 트리**를 그립니다.
기본은 접힌 상태이고, **글을 읽는 중이면 그 글이 속한 서랍만 펼쳐지며** 현재 글이 하이라이트됩니다.
소분류가 없는 글은 대분류 바로 아래에 붙습니다.

### 우측 — `TableOfContents.astro`

본문의 H2·H3를 뽑아 목차를 만듭니다. `[...slug].astro`가 `render()`의 `headings`를 넘겨줍니다.

### 헤더 — `Header.astro`

`[Home | Blog | About]` — 로고(정중앙) — `[검색 | GitHub]` 대칭 3단 그리드. 스크롤 시 고정됩니다.

### 검색 — `Search.astro`

**`Ctrl + K`** 로 열리는 팝업. 빌드 시 색인을 만들어 페이지에 심고 브라우저에서 걸러냅니다.

- 대상: 제목, 설명, 분류, 소분류, 태그 (**본문은 제외** — 색인이 커져 첫 로딩이 무거워짐)
- `#astro`처럼 `#`를 붙여도 걸립니다. 대소문자 무시, 여러 낱말은 AND
- 런타임 라이브러리 없음. 파일 하나로 완결

> ⚠️ 검색 결과 목록은 JS가 `innerHTML`로 만들기 때문에 **Astro의 scoped CSS가 닿지 않습니다.**
> `.search-results :global(...)` 형태로 열어 두었습니다. 이 부분을 건드릴 때 주의하세요.

---

## 4. 다이어그램은 SVG로

코드블록에 ASCII로 그리지 않습니다. 실측 결과 **박스문자와 한글을 섞으면 어떤 폰트에서도 정렬이 맞지 않습니다.**

| 폰트 | 한글 | 박스문자 |
| --- | --- | --- |
| `monospace` | 2.00배 | **2.00배** |
| Cascadia Mono | **1.71배** | 1.00배 |
| Consolas | **1.82배** | 1.00배 |

그래서 `src/assets/diagrams/*.svg`로 만들어 넣습니다. 4개 합쳐 **10.8KB**이고 런타임 JS는 0입니다.

SVG는 **Gemini에게 코드로 요청**합니다. `prompts/gemini_svg_diagram.md`에 규격·색상·글꼴·참고 예시가 담긴 프롬프트가 준비돼 있습니다. 생성형 이미지 모델은 그림 안 글자를 뭉개지만, SVG는 코드라서 정확합니다.

---

## 5. 과거에 있었던 함정 (재발 방지)

### ① 스키마에 없는 frontmatter는 조용히 사라진다

`tags`를 `src/content.config.ts`에 등록하지 않은 채 글에만 적어 두었더니, **빌드는 성공하는데 좌측 트리만 텅 비어** 있었습니다. Zod의 `z.object()`가 정의되지 않은 키를 버리기 때문입니다. 에러가 없어서 원인을 찾기 어려웠습니다.

**진단 요령**: 빌드 로그가 아니라 **생성된 `dist/*.html`을 직접 열어** 확인하세요.

### ② `post.slug`는 없다 — `post.id`를 쓴다

Astro 5+ 의 glob loader는 `slug`를 제공하지 않습니다. `post.slug`를 쓰면 `undefined`가 되어 링크가 `/blog/undefined/`로 생성됩니다. 이것도 빌드 에러가 나지 않습니다.

### ③ 타이포그래피는 `.prose` 안으로 한정한다

좌측 트리, 검색 결과, 대시보드 위젯도 `h2`, `li`, `a` 같은 같은 태그를 씁니다. 본문 스타일을 전역에 걸면 **UI가 함께 망가집니다.** `global.css`의 본문 스타일은 전부 `.prose` 하위로 스코프돼 있습니다.

### ④ 개발 서버가 남는다

`astro dev --background`는 **가장 최근 것 하나만** 추적합니다. 여러 번 띄우면 앞의 것이 유령으로 남아 246MB씩 잡아먹습니다. 주소가 `4322`, `4323`으로 밀리면 이 신호입니다.

### ⑤ 실행 중인 dev 서버 상태에서 `.astro` 캐시를 지우지 말 것

콘텐츠 저장소가 날아가 사이트가 빈 것처럼 보입니다. 서버를 먼저 내리고 지우세요.

---

## 6. 주요 파일 지도

| 무엇을 고치고 싶은가 | 어디를 열면 되나 |
| --- | --- |
| frontmatter 항목 추가/변경 | `src/content.config.ts` |
| 본문 글꼴·표·인용문·코드 스타일 | `src/styles/global.css` (`.prose` 블록) |
| 글 상세 페이지 레이아웃 | `src/layouts/BlogPost.astro` |
| 메인 대시보드 위젯 | `src/pages/index.astro` |
| 글 목록 페이지 | `src/pages/blog/index.astro` |
| 좌측 분류 트리 | `src/components/CategoryTree.astro` |
| 검색 동작·모양 | `src/components/Search.astro` |
| 배포 파이프라인 | `.github/workflows/deploy.yml` |

---

## 7. 남은 일

- [ ] `src/pages/about.astro` — 초안은 넣었으나 **자기소개 문단과 연락처는 직접 채워야 함** (파일 안에 `👇` 주석으로 표시해 둠)
- [ ] 3단 그리드 CSS가 3개 파일에 중복 → 공용 레이아웃으로 추출 검토
- [ ] 글이 쌓이면 태그 아카이브 페이지(`/tags/[tag]`) 필요 여부 재검토 (현재 태그 뱃지는 모두 `/blog`로 감)
- [ ] 데모용 `blog-placeholder-*.jpg`를 실제 대표 이미지로 교체

---

## 8. 다음 작업자에게

1. **글을 쓸 때는 `category`가 필수**입니다. 빠뜨리면 빌드가 멈춥니다.
2. **frontmatter에 새 항목을 추가하려면 반드시 스키마에 먼저 등록**하세요. 안 그러면 조용히 사라집니다.
3. 스타일을 손볼 때는 **`.prose` 안인지 밖인지**를 먼저 확인하세요. 밖이면 UI가 같이 바뀝니다.
4. 배포 전에 `npm run build`로 유효성 에러가 없는지 확인하세요.
5. 브라우저에서 실제로 열어 보세요. **JS로 만드는 부분(검색 결과)은 생성된 HTML만 봐서는 스타일 문제를 못 잡습니다.**
